apiVersion: v1
kind: Service
metadata:
  name: mongodb   # 👈 keep same as your URI
  namespace: chat-app

spec:
  selector:
    app: mongodb   # 👈 must match deployment label

  ports:
  - port: 27017
    targetPort: 27017

  type: ClusterIP